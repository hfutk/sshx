name: CI

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  create_release:
    name: Create nightly release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Create or update nightly release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly
          name: Nightly Build
          body: Automatic build from the main branch.
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  upload:
    name: Build and upload (${{ matrix.os }})
    needs: create_release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: arduino/setup-protoc@v2
      - run: rustup toolchain install stable
      - uses: Swatinem/rust-cache@v2

      - name: Build binary
        run: cargo build --release -p sshx

      - name: Upload binary to release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            gh release upload nightly target/release/sshx.exe --clobber
          else
            gh release upload nightly target/release/sshx --clobber
          fi
